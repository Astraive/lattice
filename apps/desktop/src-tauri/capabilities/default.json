{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum capabilities for the primary Lattice window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
