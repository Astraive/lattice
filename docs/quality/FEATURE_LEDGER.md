# Historical feature and verification ledger

**Revision:** `main` at audit start; `git rev-list --count main` reported 214 commits. The inventory is organized by capability, not by commit count. Commit subjects and changed paths were enumerated with `git log --reverse --format='%H%x09%s' main` and `git log --format='%h%x09%s' --name-only main`; the exhaustive 214-row index is [HISTORY_INDEX.csv](HISTORY_INDEX.csv), and reviewed one-to-one capability assignments are in [COMMIT_FEATURE_MAP.csv](COMMIT_FEATURE_MAP.csv). The index's subsystem tags and requirement IDs are filename/subject-derived heuristics, not reviewed feature assignments or evidence of behavior. Product behavior is not considered proven by its originating commit. Requirement IDs and intended acceptance come from `docs/REQUIREMENTS.md`, `docs/features/*`, `docs/quality/TEST_PLAN.md`, versioned candidate profiles, and the original integrated specs.

**Status vocabulary at audit start:** `PENDING-AUDIT` means no runtime acceptance claim has been established by this ledger yet; it is not a final feature state. Each row must end as exactly `VERIFIED`, `BLOCKED-EXTERNAL`, `INTENTIONALLY REMOVED` (only by product-owner authorization), or `FAILED`, with evidence. Do not convert a row to VERIFIED from code, a feature commit, compilation, or an isolated unit test alone.

| Capability | Originating history (short commit) | Subsystem / requirements | Current implementation locations | Clients / paths | Existing tests / runtime acceptance required | Audit-start state and known blocker |
| --- | --- | --- | --- | --- | --- | --- |
| Workspace, build graph, platform scaffolds | `64998b7 ebc85b0 27997a0 3bf23d4 f25badc 908d98b 577b9e3 789f85c e9d56da 4b1d541 9ed10e7 c793c14 a44459b e378390 b4c8440 8fc5bee` | Build/repo; delivery infrastructure | Cargo workspace, Bun workspace, `apps/{android,desktop,cli,ios}`, CI | Android/Desktop/CLI; iOS reference only | CI and clean native builds; build is not runtime acceptance | PENDING-AUDIT; broad integration unverified |
| Canonical encoding, event IDs, signed events, vectors and inspector | `3562485 85c3aa1 f270d96 5dbcbe9 b79f7b2 ea8482b 6fda265 dc9cf16 8f68674 d310bb0 5d00c12` | LAT-002/003/006/018; protocol/events | `crates/lattice-protocol`, `crates/lattice-events`, `crates/lattice-identity`, `packages/protocol-inspector`, `protocol/specs/{02-encoding,05-events}.md`, `protocol/vectors/canonical-cbor.json` | Shared Rust; TS inspector; Android shared vector consumption | Rust vectors, hostile decoder tests, inspector tests; clean-room Kotlin/TS/CLI parity still required | PENDING-AUDIT; candidate only, cross-binding clean-room results absent |
| Identity generation, storage, fingerprints and protected keys | `9211ed5 d8252d7 5413b14 295921f` | IDN-001/002/007/009, LAT-009 | `crates/lattice-identity`, `crates/lattice-platform`, `crates/lattice-uniffi`, Android protector/profile, CLI identity, Desktop profile | Android/CLI/Desktop; OS key stores | Identity vector/CSR/protector tests; fresh-process reopen and real storage behavior required | PENDING-AUDIT; Android hardware proof unavailable in prior environment; CSR does not issue credentials |
| Peer pinning, mismatch handling and unpin | `dd1d3d9 2bb2c5c 4f9827f 397b77f bd8d63c 8663d76` | IDN-005, CLI-010/012 | `lattice-identity/pinned_identity.rs`, `lattice-core/identity_pin.rs`, storage trusted identities, Android/Desktop/CLI pin UIs/commands | Android/Desktop/CLI | exact pin/reopen/mismatch and real authenticated-peer rejection needed | PENDING-AUDIT; persisted pin alone is not a verified session or membership |
| X.509 / RFC 9420 credential validation and PKI issuance workflow | `ca92119 e73b118 5c811f8 7ac90b9` plus audit-added `tooling/dev-pki/*`, `docs/development-pki.md` | IDN-008/009, MLS trust | `crates/lattice-mls/src/api.rs`, identity CSR APIs, CLI/desktop/Android CSR workflows | Existing Android/Desktop/CLI MLS paths | Production validator against OS trust, matching Ed25519 SPKI and canonical fingerprint SAN; valid and negative credential tests; issue CSR-derived certificates and use them through actual client flows | PENDING-AUDIT; Windows CLI positive flow and wrong-key/expired/untrusted-root rejection now exercised; Android/Desktop issuance and credential use remain unverified |
| Canonical Space genesis, durable local snapshots and recovery | `7633c5a da3d6bc 06b6d0c 951b8a9 710f665 fee197e f9581e2 66af4c4 d73be1e` | SPC-001/002, CLI-009, LAT-004/012 | `crates/lattice-core/{lib.rs,space.rs}`, storage, Android/Desktop/CLI Space workflows | Android/Desktop/CLI local | SQLite reopen/corruption, create/list/restore/recover on actual profiles; peer acceptance needed for remote claims | PENDING-AUDIT; restored scope is local Genesis/policy checkpoint, not full membership/history |
| Space policy reducer, permissions, conflicts, membership and MLS atomic transitions | `294b08d 6e8f74f ed5e808 ca92119 5f4f56c 887dc55 554133f 5f4f56c ca10562` | SPC-003–011, LAT-003/007, ADR-001/002 | `lattice-core/src/space*`, `lattice-mls`, `lattice-storage`, `protocol/specs/{06-spaces,07-permissions,08-mls}.md` | Shared Rust; app membership flows incomplete | adversarial policy/MLS permutation, exact Commit binding, valid cross-member join/remove/rekey and restart | PENDING-AUDIT; Core stores same-parent sibling evidence transactionally and fails closed after reopen; platform/client conflict recovery and interoperability remain unverified |
| Invites, KeyPackages, Welcome bootstrap, and recovery generation | `86020ad e73b118 554133f 60481d8 710f665 5c811f8` | IDN-004/008, SPC-003/006/009 | `space_invite.rs`, `space_bootstrap.rs`, `bootstrap_snapshot.rs`, MLS APIs, client commands/forms | Android/Desktop/CLI local issue/import | Independent identities through issuer/client KeyPackage/Welcome, full normal join and post-restart membership state | PENDING-AUDIT; bootstrap creation now shares the invite MLS/SQLite transaction; `SpaceInviteV1::validate_for_policy` has no production caller and token is not consumed on join; no peer delivery or end-to-end client exchange |
| Text, local encrypted cache, history, send status and edits | `ed5e808 bb56eaa 7e68660 c2e6827 370be31 3bba347 a7661d1 9cacb39 34bc00e 4f71f51 89e578b 5177c46 1d3834f 8354751 c835988 083520f 8345dc6` | MSG-001–003/006, LAT-004/012 | Core, storage, UniFFI, Android Compose, Desktop Tauri/React, CLI | Android/Desktop/CLI local outbox; direct sync integration | Actual client-generated send→authenticated receive→Core projection→durable history/restart; mutations across clients | PENDING-AUDIT; local queue is not forwarding/delivery, incoming paths not end-to-end in apps |
| Tombstones, reactions, pins, mentions, rich text, parent dependencies, presence/typing, local mutes | `8663d76 d49a03a d87dc64 8b0ba28 7472592 f50d9a6` | MSG-005–012, SPC-008 | `lattice-core/src/space*`, `lattice-events`, storage | Core plus limited Android/Desktop/CLI surfaces | Cross-client authorized mutation, reorder/duplicate/child-before-parent repair, ephemeral TTL/replay tests | PENDING-AUDIT; Core authorization/projection covers target ancestry, tagged removals and duplicates; the full mutation graph is in-memory and the durable text cache is narrower; app-level projection/replay remains unverified |
| Device-scoped DM MLS groups | `2d9ca6a` | MSG-004, IDN-005/008 | `crates/lattice-mls/src/api.rs` | MLS primitive only; client messaging integration needed | Actual Android/Desktop/CLI member exchange, remove/rekey, restart and nonmember rejection | PENDING-AUDIT; exact two-device MLS pair creation, Welcome join, pair binding, encryption, and peer-removal closure pass one unit scenario; no Core, event/outbox, storage integration or product callsite exists |
| Search and retained-history query | `6a5adb7 537bb82 9bc5b25` | MSG-011 | Core/storage, Android UniFFI/UI, Desktop Tauri/React, CLI | Android/Desktop/CLI offline local cache | Real retained/edited/deleted cross-client received history, isolate Space, restart persistence | PENDING-AUDIT; search is local cache only, remote history absent |
| Attachment manifests, receivers, staging quotas, verified chunks | `ec44041 fc238c1 4d872da 8667635 d6ee58f eb39227 0c03eec 21f26c5 5c6ff59 95f0001 2ed2a32 ee2f787 417aa0e` | FIL-001–007, LAT-006 | `lattice-files`, Core manifest transactions, staging storage, node transfer | Rust Core/desktop transfer; Android and Web absent | Real sender→receiver transfer, consent, corrupted chunk/unsafe path rejection, interruption/restart/resume, cross-client hashes | PENDING-AUDIT; Android pipeline and broader event synchronization not wired |
| Bounded routing, path capability offers, path health and TCP framing | `b073887 86f4d58 63c3ace 24d86ee 982bfa3 53147a8 be0582b 4f15d06 7350b8b` | NET-006/007/013, LAT-011/016 | `lattice-router`, `lattice-transport`, `lattice-protocol/path_upgrade.rs`, `lattice-node/path_health.rs` | TCP primitives; desktop sockets; Android probes only | Real authenticated path negotiation, path outage/fallback and transfer; remove a path mid-operation | PENDING-AUDIT; docs say no platform LAN/Wi-Fi path adapter consumes negotiation |
| Direct authenticated sync v1/v2, scoped summaries/ranges, store-backed repair | `6a6a9bb 9a39972 5f23886 a45afa9 bcbcbc4 31be12c 2e6725a 48722a3 206fe4a 9e7d7b6 4e4803e c40a6d9 9f710c2 dfd2ce3 f3518a0 c43b233 9f96085` | NET-005/012, LAT-002/003/007 | `lattice-node/src/sync*`, `lattice-sync`, Core receive validation, CLI sync | CLI↔CLI TCP currently; Android/Desktop use their own local commands | Two actual isolated profiles, bidirectional authenticated reconcile, missing dependencies/parents, reordering, duplicate, restart and equal accepted history | PENDING-AUDIT; one session is bounded/partial by design; cross-app binding and complete convergence need evidence |
| CLI identity, Space, local send, history/search, relay config, sync/node JSON | `ec046a2 370be31 4f9827f 5c811f8 9bc5b25 3bba347 2e6725a 206fe4a 9e7d7b6 4e4803e c9c11d3 bf392fa` | CLI-001–012 | `apps/cli/src/*`, `apps/cli/tests/*`, core/node | CLI local plus authenticated TCP peer functions | Actual profile restart and send/receive/reconcile with other client classes, test stable JSON contracts | PENDING-AUDIT; CLI reports several unavailable capabilities; OS keyring profile and data-dir isolation need end-to-end check |
| Desktop identity, local Space/message UI, history/search, local paths, relay settings | `a740323 7c337d6 187a005 c2e6827 0be643f 6a5adb7 f9581e2 b924ce1 7967549` | DSK-001–007 | `apps/desktop/src`, `apps/desktop/src-tauri/src/*` | Desktop local; TCP transfer/node primitives | Run packaged/native application with separate profiles and communicate with CLI/Android; restart durable behavior | PENDING-AUDIT; code presence/build not cross-client proof |
| Shared desktop theme and status components | `d273f7f` | DSK UI semantics; cross-cutting presentation | `packages/theme`, `packages/desktop-ui/src/components.tsx` | Shared React components; no production consuming surface confirmed | Package tests cover textual delivery labels/accessibility and untrusted text escaping; verify the actual Desktop/browser surfaces consume the tokens/components and accurately report app state | PENDING-AUDIT; components are not established as wired into a product surface |
| Courier queues, pinned TCP forwarding and relay outbox | `732aa21 4d91ef8 0d83e5c c9fbb74 b924ce1 7967549` | NET-004/008/011, CLI-007, DSK-005 | storage courier queue, node courier/relay, desktop peer mode, CLI node | TCP courier, optional relay | A→B carry→C; quota/expiry/duplicate/failure preservation and unchanged event id | PENDING-AUDIT; CLI consumption-on-send can lose a copy after failure by documented policy; relay interoperability absent |
| NIP-01/NIP-11/NIP-40 relay codec and client | `7d2fbb2 b6ce5a9 6e8f74f 48b646f 0d83e5c f62d1c0` | NET-009–011/013, ADR-003 | `crates/lattice-relay`, node relay, client settings | Optional secure HTTP/WebSocket relay | Two independent relay services, publish/retrieve/drop/reorder/duplicate, actual end-client application receive | PENDING-AUDIT; current accepted profile is candidate; docs expressly deny independent relay interoperability |
| Android Compose identity/Space/messages/diagnostics/permissions/accessibility | `7c337d6 5413b14 7e68660 fee197e 5177c46 537bb82 60481d8 003968a 4bcce70` | MOB-001/002/008/009/010/011 | `apps/android/app/src/main/kotlin`, UniFFI | Android local/Core; Bluetooth scan/GATT components | Actual app/Core/SQLite path, accessibility, profile/process restart, notification authorization | PENDING-AUDIT; no channel observations; notifications are service status only; app backend not connected |
| Android BLE codec, scanning, GATT primitives and nearby service | `a092dda 902018e 4389c4c b944e45 9159caa` | MOB-002/003/006/007/008, NET-001/002, ADR-005 | `BleFrameCodec.kt`, scanner, GATT adapters, foreground service, manifest | Android BLE only | Physical two/three-device offline message exchange with authenticated Core ingestion | PENDING-AUDIT; scan/GATT primitives do not advertise/authenticate/connect/exchange Lattice messages; hardware adb unavailable earlier |
| Android UI selection and native key status | `4bcce70 295921f 003968a` | MOB-009/013 | Compose screens, tests, docs | Android native UI | Screen-reader/permission/lifecycle review on real supported devices | PENDING-AUDIT; only prior emulator evidence documented |
| Voice signaling, lifecycle and permission termination | `ec0e5a3 b7a46bc fc531b5 56e6f96 64d4ec9 7ac90b9` | VOC-001/002/006/007/008 | `crates/lattice-voice`, voice docs | Rust signaling state machine | Deterministic state/error/deadline/revoke tests | PENDING-AUDIT; signaling is not a media call |
| Voice native media, audio adapters, WebRTC/Opus/ICE/TURN | `ec0e5a3` plus documented `VOC-001–008` | VOC-003–008, LAT-017 | No WebRTC dependency, Android AAR/ABI or desktop native artifact identified in docs; no WebRTC media integration | Android/Desktop required | Actual audio calls and network matrix | PENDING-AUDIT; docs report native artifacts/build toolchain and platform audio/permission integration missing; inspect current platform caches before external classification |
| Testkit, property/campaign and fuzz harnesses | `14362e5 dd599a8 86f8883 d6abf7b 61159e5 a612601 dc9cf16 6fda265 ea8482b be0582b 9f96085 f3518a0` | LAT-006/007/018 | `crates/lattice-testkit`, isolated `fuzz/`, `docs/quality/TEST_PLAN.md` | Rust tools | Enumerate targets and actually execute bounded campaigns on supported toolchain; capture runs | PENDING-AUDIT; harness existence does not prove campaigns; no fuzz targets yet for BLE/invite/MLS/voice |
| Documentation, requirements/status corrections, architectural decisions | `343b314 567922a 329499a 1c98f49 c6b553f f105d3a 887dc55 3ec8056 237b3b8` | Cross-cutting | docs/README, PLAN, TODO, requirements, spec, feature docs, architecture, ADRs | All | Compare documented maturity and requirements against runtime paths; historical docs may be superseded but not silently ignored | PENDING-AUDIT; docs deliberately say no end-to-end release |
| Web application and browser Lattice client | No originating Web feature commit found in 214 commits | Newly required cross-client scope; no WEB registry IDs currently | `apps/web` absent; existing `packages/protocol-inspector` is not an app or Lattice peer | Browser-compatible path required | Real browser local identity, event/auth/MLS/store/sync exchange with Android/Desktop/CLI and Web↔Web | PENDING-AUDIT; no Web client or browser-compatible peer path exists in current source |

## Requirements and document reconciliation (initial)

- `docs/README.md`, `docs/REQUIREMENTS.md`, PLAN and TECH_STACK scope the current clients to Android, Desktop and CLI; iOS is out of scope. The user's current objective adds Web as required scope, so a Web client and traceable Web requirements must be introduced without pretending it already exists.
- Feature catalog owners are identity (IDN), spaces (SPC), messaging (MSG), networking (NET), files (FIL), voice (VOC), mobile (MOB), desktop (DSK), and CLI. Cross-cutting LAT requirements add offline/no-account behavior, event identity across re-enveloping, authorization-before-projection, truthful delivery states, non-authoritative carriers, bounded resource use, deterministic projection, secure storage, privacy, version negotiation, accessibility, optional infrastructure, migrations and measured claims.
- Protocol documents/spec files call themselves candidates/drafts and prohibit an interoperability claim. ADR-001/002 are accepted policy decisions but still require behavioral evidence; ADR-003 is a candidate profile without two-relay proof; ADR-004/005/006/007 are open/deferred scope, not hidden implementation requirements to silently change.
- Current accepted implementation is mostly a set of bounded Rust component candidates; local Space genesis, offline authored queue and some local queries exist. Production communication across app surfaces is not established. `docs/features/mobile.md` explicitly says GATT does not advertise/authenticate/exchange; Desktop docs constrain sync/transfer claims; CLI docs state local send and local Welcome artifacts do not imply delivery/membership; voice docs identify missing native SDKs.
- `docs/TODO.md` is not sufficient as an inventory; it omits historical feature commits and newly required Web/cross-client behavior. This ledger is additive traceability and does not mark old TODOs complete.

## Initial current-source mapping observations

These are read-only source-path findings, not runtime acceptance evidence. All related capabilities remain `PENDING-AUDIT` until their specified behavior is exercised.

- **Protocol bindings (initial finding resolved for the candidate inspector):** `EventKind::Ephemeral = 10` and non-contributory X25519 identity keys now have matching Rust/TypeScript validation, with a shared candidate signed-event vector. Inspector byte, string, collection, and nesting bounds now match `02-encoding.md`. Tests establish vector behavior, not client transport interoperability.
- **Core and MLS:** Core has real signed/encrypted local event storage, outbox and authorization/projection boundaries. Message-history reducer state is in-memory; local durable text cache is narrower than the reducer. MLS production credential admission requires OS-rooted X.509 chain, matching Ed25519 SPKI and canonical identity SAN. MLS conflict markers retain exact signed controls, persist with received events, and are revalidated on local restore; invited-device restoration requires its Welcome checkpoint. Welcome bootstrap is a local pinned-inviter checkpoint, not live invite delivery. Invite token policy validation has no production callsite.
- **Sync and carriers:** CLI binds bounded authenticated v2 sync to Core acceptance and has reciprocal reconciliation; the single-session v2 exchange is bounded to one repair batch. Desktop has no sync/reconcile command. TCP is a byte carrier. Router planning has no production caller. Courier queue/TCP primitives have manual CLI/Desktop callsites; relay publish/retrieve APIs have no product callsite and no received-event-to-Core path.
- **Android:** Compose reaches local UniFFI/Core operations and the scanner; GATT central/peripheral adapters are not instantiated, there is no advertiser, and no GATT-to-Core bridge or Core projection observer is wired. The persistent nearby service reports scan status only.
- **Voice:** `lattice-voice` exposes a local signaling state machine and explicitly returns `MediaStatus::NotImplemented`; no production consumer or Android/Desktop media backend is present in the audited paths.
- **Scaffolds:** `tests/{e2e,interop,mesh,mobile,protocol,routing,security,sync}`, `tooling/{benchmarks,fuzz,mesh-simulator,network-chaos,packet-inspector}`, `design/{assets,icons,scripts,tokens}`, `protocol/schemas`, and root `transports` are placeholder-only. `fuzz/fuzz_targets` contains six Rust targets: canonical, signed event, identity bundle, attachment manifest, Nostr event and relay envelope. `protocol/specs` and `protocol/vectors` contain actual files plus stale `.gitkeep`.
- **PKI tooling:** the example now uses accepted `space create --credential ... --channel general` flags. On Windows, the development CA/leaf/vector scripts were exercised with PowerShell 7.6.6 and OpenSSL 3.6.0 against the production CLI validator using Current User Root trust; positive local Genesis and wrong-key, expired, and untrusted-root rejection passed. Android/Desktop trust bootstrap remains unverified.

- **SQLite transactions and outbox (source audit):** schema migrations 1–13 apply each DDL change and `user_version` update in one `IMMEDIATE` transaction; open configures WAL, foreign keys, and a busy timeout, and rejects newer schemas. Event bytes, parent links, pending-row deletion and optional outbox insert are transactional; Core's SQLite-backed OpenMLS provider shares the authored-event transaction. Persisted outbox bounds are currently applied to every row and all envelope bytes, including terminal `delivered`/`failed` records; those states are never pruned. Therefore the nominal queue limits can become a lifetime send limit after 1,024 retained envelopes or 16 MiB, even after delivery/failure. The architecture specifies outbox retention only until acknowledgment/expiry/retry end, so terminal-state cleanup/status retention needs a deliberate lifecycle contract before changing behavior. Migration crash atomicity follows SQLite transaction structure; process-kill crash recovery was not exercised.
- **Authenticated synchronization (source audit):** the deterministic planner rejects cross-summary sequence forks, bounds author/event/gap/request/dependency sets, requests exact pending dependency IDs before sequence ranges, and reports explicit retention gaps rather than claiming completeness. Direct v2 sync binds Noise to the pinned identity, scopes both summary and event source to one selected Space generation, validates event signatures/metadata on source and response, then hands bytes back to Core for MLS/policy admission; CLI reconciliation persists accepted events, retries pending dependencies, and repeats fresh summaries in bounded rounds. The Store summary source verifies stored records and fails on its budget, but it scans/verifies the profile-wide event table before filtering the selected generation; >16,384 profile events or >16 MiB scanned bytes can prevent syncing a small scope. Existing test coverage exercises planner cases and one authenticated TCP range-repair exchange; it does not exercise cross-process CLI reconciliation, out-of-order parent/child repair through the full CLI/Core path, or multi-round convergence/restart.
- **Routing, path negotiation, and health (source audit):** route planning applies caller-supplied reachability, capability, internet/metered, energy, loss, queue, RTT, MTU/frame, expiry, hop/copy/fanout and traffic-class policy, with deterministic class-specific ranking. Authenticated path-upgrade offers exchange a versioned canonical-CBOR intersection only after pinned Noise identity proof and caller authorization; comments correctly state this does not discover or open a path. `PathHealthTracker` records bounded local lifecycle, exact-hop send outcomes and successful receives without inferring recipient delivery. No production caller was found for `plan_forward`, `PathHealthTracker`, or authenticated capability negotiation; Android only probes and displays local Wi-Fi availability. Thus route fallback, failure-driven path switching, or operational health-based planning are not wired into an app.
- **Courier queue and forwarding (source audit):** persistence is disabled by default; explicit configuration is bounded by hard object/peer/total byte and item ceilings, expiry, event/envelope deduplication, FIFO quota eviction, and transactional budget consumption. One transfer authenticates an exact pinned peer over Noise, validates the signed envelope/expiry/budgets, moves it in bounded 60,000-byte chunks, and acknowledges only after local receiver queue retention. CLI and Desktop expose manual pinned TCP listener/forward operations. Forwarding decrements hop/copy budget and deletes the source row after peer authentication but before the first transfer frame; a later disconnect or remote queue rejection permanently loses that queued copy. This is documented policy, not failure-preserving retry. Tests establish queue quotas/reopen and one-hop TCP transfer/pin mismatch; no three-node forwarding, post-consumption disconnect, or automatic carry/retry was exercised.
- **Optional relay transport (source audit):** the candidate profile separates the Nostr relay signing key and private mailbox token from the Lattice device identity; outer kind/tag/expiry, canonical envelope ID, inner signed event, and expiry are checked together. NIP-01 verification rejects invalid IDs/signatures/duplicate or unknown fields; NIP-11 has bounded duplicate-key-rejecting parsing and publishing requires NIP-40 plus a sufficient advertised frame size. HTTPS redirects are disabled; operations require `wss://`, finite timeout/frame/aggregate/event bounds, exact positive `OK` for publish, exact mailbox retrieval, and cancellation/close handling. Relay acceptance is recorded as `Forwarded`, not `Delivered`. CLI currently probes NIP-11 only; no application caller for outbox publication or retrieval was found, and `network.rs` tests cover URL/filter/frame parsers rather than a live or mock HTTP/WebSocket exchange. No independent relay pair or end-client receipt path was exercised.
- **Attachments, transfer authorization, resume, and cleanup (source audit):** manifest events are policy-authorized and atomically queued as metadata/digests; bytes stay in local staging. Direct transfer uses a pinned Noise session, event-bound transfer ID, caller authorization, explicit receiver consent, bounded chunk frames, per-chunk and whole-file SHA-256 verification, and final ACK only after full verification. The Desktop UI is wired to select/cache, manually send/receive, consent, and export via non-overwriting temporary files; source and staging quotas are bounded, received chunks are resumable after reopen, and expired/orphan staging is cleaned. Desktop caps files at 128 MiB with 512 MiB/64-transfer staging quotas and seven-day retention; Android and CLI have no transfer UI. `cargo test -p lattice-files` passed 16 tests; `cargo test -p lattice-node attachment_transfer` passed 3 scenarios (authenticated transfer/resume, consent rejection, silent-peer timeout). Evidence does not cover a process-kill transfer resume end-to-end, Desktop UI runtime, or Android transfer.
- **Search, ephemeral presence/typing, and local mention mutes (source audit):** offline search decrypts only the bounded local per-channel cache, normalizes with Unicode lowercase substring matching, returns at most 100 newest matches, and re-verifies signatures/metadata for each returned event; Desktop and CLI invoke it, while Android has generated UniFFI but no product callsite was found. Remote authorized edits replace cached content. Remote authorized tombstones previously left the encrypted text cache row searchable; Core now deletes the scoped cache row transactionally with accepted tombstones, and the received edit/tombstone scenario asserts both search and local history exclude it. Presence/typing payloads are canonical and bounded (60-second presence, 10-second typing), keyed per Space generation/member/kind, active-member/conflict checked, replay-sequenced and monotonic-time expired in an in-memory table; tests cover duplicate/stale sequence and expiry, but no production sender, receiver, or observer uses the table. Mention mutes are encrypted device-local identity/role target preferences (4,096 bound) and survive Core profile reopen; notification eligibility takes already-resolved local targets, but no app binding or notification consumer was found.
- **Security negatives before projection (source audit):** `accept_synced_application_event` verifies the signed event, checks exact selected generation and dependencies, processes/binds the MLS application, runs policy authorization, and only then persists the event and local text projection in one SQLite/OpenMLS transaction; non-authorized results become errors before projection, with the transaction uncommitted. Existing tests reject MLS author/ciphertext/epoch/group/fingerprint mismatches before releasing plaintext; reducer tests reject nonmembers, denied channel permissions, incomplete heads, invalid targets, and authorization before event storage. One direct storage-path negative asserts denied signed/MLS-bound input has no event row. No end-to-end transport-delivered invalid sender/group/permission test currently asserts both durable history and search remain empty; system-level security acceptance is still pending.
- **Bounded fuzz campaigns (Windows):** added a structured `sync-plan` libFuzzer target exercising per-author sequences, same-sequence event-ID disagreements, gaps, and dependency inputs; documented it in the test plan and registered the `lattice-sync` dependency. With nightly Rust, default AddressSanitizer, the installed MSVC `clang_rt.asan_dynamic-x86_64.dll` on `PATH`, `ASAN_OPTIONS=quarantine_size_mb=64:thread_local_quarantine_size_kb=256`, and `-max_len=4096`, bounded campaigns completed without crash: canonical 661 executions, signed-event 641, identity-bundle 127,458, sync-plan 32,807, relay-envelope 1,109,096, and nostr-event 1,265. These are short smoke budgets, not release-strength coverage; Windows MSVC cannot run this harness with `--sanitizer none` because the coverage-section linker symbols remain unresolved.
- **BLE profile/migration boundary:** `protocol/specs/10-ble.md` assigns `pre-profile`, `lattice-ble-exp0`, and reserved `lattice-ble-v1` labels; exp0 is metadata, not an on-air string or interoperability claim. Candidate exp0 UUIDs cover one service and five characteristics; its 31-byte legacy advertisement carries discriminator `0` and a fresh 72-bit random token rotated every 900 monotonic seconds, with sightings memory-only and capped at 1,024. The candidate handshake uses Noise XX plus post-Noise Ed25519 proofs binding both exact identity bundles, observed discovery token and session transcript; first contact requires matching a 48-bit session string and explicit full-fingerprint pin. Android still scans the old pre-profile UUID, deduplicates session-salted device-address digests rather than tokens, and has no advertiser or authenticated GATT message path. Vectors, ADR-005 review, platform wiring, and physical acceptance remain open.

Source-only audit lanes are being consolidated against exact production callsites, tests and requirement provenance; none of these observations imply an end-to-end pass.

## Audit run log

| Pass | Revision | Scope | Command/scenario | Evidence | Result |
| --- | --- | --- | --- | --- | --- |
| Initial inventory | `main` at start | Full branch subject/path index and documentation map | `git rev-list --count main`; `git log --reverse --format='%H%x09%s' main`; `git log --format='%h%x09%s' --name-only main` | 214 commits; full subject/path output reviewed; docs and current candidate profiles read | Historical index enumerated; commit-level runtime validation not yet done |
| Windows PKI smoke | `main` at audit start | Development CA, CSR-derived Ed25519 leaf, TLS certificate vector, native trust and production credential validation | PowerShell 7.6.6; OpenSSL 3.6.0; temporary CLI profiles; `space create --credential ... --channel general` | Current User Root installed then removed; the CLI created a local Genesis; trusted wrong-key, expired and untrusted-root credentials produced expected validation failures; scratch profiles removed | Positive and three negative validator paths passed on Windows CLI only; Android/Desktop remain unverified |
| Candidate event and decoder conformance | `7967549` plus audit changes | Event kind/version, identity bundle and canonical-CBOR bounds | `bun test` and `bun run check` in `packages/protocol-inspector`; `cargo test -p lattice-protocol -p lattice-events` | Kind-10 vector verifies in TypeScript and Rust; kind 11 and low-order X25519 rejected; 16 inspector tests and 19 Rust tests passed | Candidate parser/vector evidence only; no client-to-client transport claim |
| Identity storage and peer pins | `7967549` plus audit tests | OS-backed wrapping, exact full-fingerprint pins, persistence and restart | Core identity initialization/reopen and peer-pin restart tests; `cargo test -p lattice-platform os_keyring_protector_tests`; Windows PKI CLI workflow; Android protector unit test attempted | 2 Core restart/pin tests passed; 7 key-protection tests passed; Windows CLI kept the initialized identity across separate commands; Android unwrap callback output is zeroed after RustBuffer copying | Android test could not start because `JAVA_HOME` and `java` are absent; Desktop/Android process restart and Android hardware key backing remain unverified |
| Space invite, Welcome, and rollback boundary | `7967549` plus audit changes | Invite signer failure atomicity, package version, join and restore semantics | `cargo test -p lattice-core` | 53 Core tests passed; invitation test rejects zero-expiry after staged MLS Add and retains expected author sequence/outbox before succeeding; signed unsupported Welcome package version rejected; package signing/encoding now occurs before the shared MLS/SQLite transaction commits | Local Core behavior verified; invite token validation is not called in production and actual cross-client delivery remains unverified |
| MLS sibling-Commit conflict persistence | `7967549` plus audit tests | Validate two same-parent membership Commits, retain neither branch, persist exact event evidence and fail closed after reopen | `cargo test -p lattice-core` | Core test creates competing valid branches, stores both signed controls and conflict marker, rejects later membership mutation, and reopens the profile with the generation still immutable; source restore replays from prior accepted epoch and revalidates exact controls | Durable local conflict-latch behavior verified; full inviter-issued Welcome-profile restart restoration and physical client recovery remain unverified |
| Messaging mutation authorization and projection | `7967549` plus audit tests | Signed/MLS-bound send, edit, tombstone, reaction, pin, dependency, duplicate and local-cache behavior | `cargo test -p lattice-core` (53 passed); targeted `application_authorization_uses_policy_heads_action_bits_and_target_causality` and `pinned_welcome_bootstrap_joins_and_restores_checkpoint_policy` | Target/action/permission/causal rejection tests passed; integration test covers exact duplicate, pending dependencies, remote edits/tombstones and tagged reaction/pin removal; local edit and text cache survive profile restart in the full Core run | Core candidate behavior verified for these test paths; durable replay of full remote mutation graph and consuming UI paths remain unverified |
| Direct Message MLS primitive | `7967549` plus audit | Caller-pinned two-device pair, KeyPackage/Welcome, application encryption, peer removal | `cargo test -p lattice-mls direct_message_group_is_pinned_to_two_devices`; source search for `DirectMessageGroup` across Core, app, UniFFI and node paths | One MLS test passed: creates exact pair, imports and reloads group, authenticates application data, rejects mismatched peer fingerprint, then closes both sides after removal | Primitive behavior verified in isolation; identity/session event integration, persistent conversation metadata and product callsites are absent |
| Storage transaction, migration, and outbox audit | `7967549` plus audit | Migration atomicity, event/outbox transaction, terminal-state bounds, crash recovery | `cargo test -p lattice-storage` (33 passed); existing `openmls_group_and_event_share_a_durable_transaction` Core test; source review of migrations, Store transaction boundaries and outbox transitions | Migration tests preserve events for legacy versions; SQLite transaction tests cover rollback and reopen; authored event/outbox caps count every status while terminal rows persist; no process-kill crash scenario was run | Local transactional behavior tested; lifetime capacity saturation after terminal outcomes and crash recovery are unresolved; no system-level durable delivery claim |
| Authenticated sync, dependency repair, and convergence | `7967549` plus audit | Scoped summaries, exact dependency/range repair, pinned direct sessions, convergence | `cargo test -p lattice-node store_summaries_drive_authenticated_v2_range_repair` (1 passed); `cargo test -p lattice-sync` (8 passed); source review of CLI reconcile and Core pending retry | Planner rejects sequence forks, requests missing dependencies before ranges, exposes gaps; one TCP v2 test verifies source-scoped signed-event exchange; CLI code accepts/retries Core events over at most eight rounds | Component and source behavior verified; no process-level CLI convergence or cross-client sync proof; summary source global scan budget can block one scope due to unrelated profile history |
| Courier opt-in, quotas, failure behavior, and forwarding | `7967549` plus audit | Opaque persistence, quotas/expiry/dedup, authenticated transfer, hop/copy bounds, multi-hop | `cargo test -p lattice-storage courier_queue` (3 passed); `cargo test -p lattice-node --test courier_transfer` (2 passed); CLI/Desktop callsite review | Queue tests verify disabled-by-default, persistence, budget consumption, duplicate rejection, expiry and FIFO eviction; TCP test verifies exact pin and single-hop envelope identity/budget plus receiver persistence; wrong pin leaves source queued | Local queue and one-hop behavior verified; source is consumed before transfer and can be lost on post-auth failure; multi-hop and failure-preserving relay unavailable/unverified |
| Router selection, path negotiation, and health | `7967549` plus audit | Route constraints, capability intersection, exact-hop health, fallback integration | `cargo test -p lattice-router` (11 passed); `cargo test -p lattice-node path_health` (2 passed); `cargo test -p lattice-protocol path_upgrade::tests` (3 passed); callsite search across `apps` and `crates` | Route policy and ranking unit suite passed; path health tests preserve exact receipt/failure and reject stale/unknown/capacity overflow; offer tests check intersection and malformed/unsupported limits; no production route/health/negotiation callsites found | Component behavior verified in isolation; no path adapters/fallback integration or real path failure scenario; capability offers do not establish reachability |
| Relay codec, publish/retrieve, and independent-service audit | `7967549` plus audit | NIP-01/11 and opaque profile validation, bounded WSS operations, exact mailbox publish/retrieval, service interoperability | `cargo test -p lattice-relay` (24 passed); source review of node relay method, CLI commands, and all app callsites | Codec/profile and parser tests passed; local node method maps exact relay acceptance to Forwarded only; no product publish/retrieve callsites; network tests are parser/URL/filter unit tests, not live service tests | Candidate codec behavior verified locally; no WebSocket/HTTPS network smoke, two independent relay services, relay-backed peer receive, or delivery receipt integration |
| Attachment authorization, transfer, resume, and cleanup | `7967549` plus audit | Authorized manifest event, pinned transfer, consent, chunk/whole-file integrity, resumable staging and removal | `cargo test -p lattice-files` (16 passed); `cargo test -p lattice-node attachment_transfer` (3 passed); source review of Core reducer, Desktop Tauri commands/UI and staging cleanup | Authenticated socket scenario resumes a preverified chunk; consent rejection and silent-peer timeout reject; filesystem staging quota/reopen/cleanup covered by crate suite; Desktop checks manifest authorization and exact pin before transfer and exports only verified bytes | Local mechanisms tested; no process-kill network resume or actual Desktop UI run; Android/CLI transfer paths absent; cross-client delivery unverified |
| Search, ephemeral presence/typing, and local mention mutes | `7967549` plus audit changes | Bounded local search; tombstone privacy; ephemeral expiry/replay; encrypted local preference persistence | `cargo test -p lattice-core` (53 passed); callsite search across `apps` and `crates` | Remote-message integration covers incoming text, edit and tombstone; regression asserts tombstoned edited content disappears from both search and history; existing Core tests cover bounded case-insensitive search, encrypted mute persistence/reopen, and ephemeral duplicate/stale/TTL behavior | Local Core behavior verified; no client presence/typing path or notification/mute UI consumer; Android search has generated binding but no usage; cross-client history and behavior remain unverified |
| Security negatives before authorized projection | `7967549` plus audit | Signed event and MLS binding, Space/member/permission/causal authorization, storage/projection order | `cargo test -p lattice-core` (53 passed); source review of `accept_synced_application_event`, `authorize_and_store_application_event` and projection transaction | Tests reject wrong author/ciphertext/epoch/group/fingerprint binding, nonmember senders, denied channel permission, invalid action/target/heads; storage-bound authorization scenario confirms denied event has no stored row; source confirms Core calls cache persistence only after `Authorized` and in same transaction | Core negative checks and source ordering verified; integrated malicious network input with explicit no-history/no-search postcondition not yet exercised |
| Bounded protocol/event/identity/sync/relay fuzz campaigns | `7967549` plus audit changes | Canonical CBOR, signed events, identity bundles, sync planner, relay envelope and NIP-01 event decoders | `cargo +nightly fuzz run` per target, `-max_total_time=2–3`, `-max_len=4096`, Windows MSVC AddressSanitizer; `cargo +nightly fuzz list --fuzz-dir fuzz` | Clean bounded runs: canonical 661, signed-event 641, identity-bundle 127,458, sync-plan 32,807, relay-envelope 1,109,096, nostr-event 1,265 executions; sync target expands at most 32 author summaries and checks bounded/deterministic plans and dependency precedence | No crashes in these smoke campaigns; short budgets do not establish security or cover unimplemented BLE/invite/MLS wrapper/voice fuzz targets |
