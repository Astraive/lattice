[package]
name = "lattice-node"
description = "Lattice optional persistent peer"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
lattice-events = { path = "../lattice-events", version = "=0.1.0" }
lattice-storage = { path = "../lattice-storage", version = "=0.1.0" }
lattice-crypto = { path = "../lattice-crypto", version = "=0.1.0" }
lattice-identity = { path = "../lattice-identity", version = "=0.1.0" }
sha2 = "=0.10.9"
lattice-platform = { path = "../lattice-platform", version = "=0.1.0" }
lattice-router = { path = "../lattice-router", version = "=0.1.0" }
lattice-sync = { path = "../lattice-sync", version = "=0.1.0" }
lattice-transport = { path = "../lattice-transport", version = "=0.1.0" }
thiserror = "=2.0.20"
tokio = { version = "=1.53.1", features = ["macros", "rt"] }
tokio-util = { version = "=0.7.19", features = ["rt"] }

[lints]
workspace = true
